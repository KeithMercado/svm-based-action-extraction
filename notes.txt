TODO: 
1. generate a short informative classroom tasks and convert as mp3 to use as dataset.
2. Fix the system audio recording (currently only mic audio is captured)
3. feed 5-10 datasets of videos or mp3 file to train the svm model and check the accuracy of the model.
4. create a separate file similar to main.py but separate the models for each files and phases.

-- 2/26/26 --



-- 3/3/26 --
Observation:
BOTH ARE GOOD
- System recordings are more than average
- Paraphrase is more than average, it is good for task assigning (3rd person pov)

TO BE FIX:
- SYSTEM audio
- REAL TIME TRANSCRIPT

if the requirements is only a system audio > it is much easier to do a real time transcript. 


TODO:
 - add tfidfvectorizer from scikit learn. (learns all data automatically(Taglish if needed)) *not prio
 - check all the daemon thread to ensure that if the user close the windows, the recording will stop immediately 
 - model versioning: name the pkl model with the version or date. (para if ever tanungin tayo kung ano or gaano nagimprove yung model natin)
    - we can say na "Between February and March, our model's accuracy improved by 15% as we added more training data"

- file upload feature (explore file manager)
- remove folder button and change to pdf convert button
   > IF VIDEO IS NOT FOUND IN THE APP, EXPLORE THE SYSTEM FILE EXPLORER.
   > CHOICE TO CONVERT TO PDF




-- 3/4/26 --
GOAL IS TO SEPARATE THE MAIN INTO A CLEAN FILE MANAGEMENT
- BREAK DOWN other components into methods if needed.
- ADD folder for specific components if needed.

- looping process of mic and file
- but the result are the same and as expected.

SOLVED:
- Windows is crashing when transfering the recorded file into a pdf or transcripted result. (SOLVED)
- prevents memory leaks
- prevents race condition crashes
- potential overflowing is fixed
- added threading for better laning and separation of process.
- prevents the application to create or trying to copy a file into itself.


----------------
3/29/26
DONE:
   1. Cleaned datasets and a short data quality report
   2. Same row counts reproducible every run.
   3. Best model selected with evidence.
   4. Map labels strictly: information_item/0 -> 0, action_item/1 -> 1.

TODO:
SYSTEM BASED:
fix system while training dataset.
   1. Fix audio capture requirement gap (system audio + mic)
	- Decide capture mode: mic only, system only, or mixed.
	- Implement proper system audio capture path (current audio_handler.py comments already identify this gap).
	- Add device selection UI and fallback behavior when loopback/system device is unavailable.
	- Validate with 5 known test clips and 3 live sessions.

Deliverables:
	Reliable capture for the required source (especially system audio).
	- Test log proving captured audio matches expected source.


   2. Stabilize end-to-end runtime and evidence output
   - Standardize model load path across app and scripts so runtime always loads the intended latest model.
	- Add runtime logging for: selected model, selected dataset version, transcription mode, and processing time.
	- Add one-click export of session summary + extracted actions + metadata for thesis appendix.
	- Run a stress test (multiple start/stop cycles, long recording, export loop) and document failures fixed.

Deliverables:
	- Stable demo pipeline from record/process -> action extraction -> export.
	- Reproducible run logs for thesis defense questions.

